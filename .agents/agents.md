# Antigravity Agent Rules

- never invent dataset labels
- never fabricate metrics
- never claim an experiment succeeded without running it
- never hard-code absolute paths
- never expose API keys
- always write tests for important logic
- always explain breaking changes
- prefer small modular files
- preserve reproducibility
- do not modify unrelated files
- before large architectural changes, explain the plan
- after implementation, run relevant tests
